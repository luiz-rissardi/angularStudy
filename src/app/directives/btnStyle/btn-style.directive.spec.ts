import { BtnStyleDirective } from './btn-style.directive';

describe('BtnStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnStyleDirective();
    expect(directive).toBeTruthy();
  });
});
